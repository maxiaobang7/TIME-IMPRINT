# Website Release Plan

Goal: publish the current web app to maxiaobang7/TIME-IMPRINT with reproducible setup and authentic demonstrations.

Architecture: retain the React/Vite frontend and local Canvas processing. No backend, new product features or changes to template designs.

1. Archive legacy iOS sources, old reports and unused assets locally; exclude archives, design originals, generated output and secrets from Git.
2. Fix case-sensitive test entry path; put the PWA manifest in public so the production build includes it; declare direct development dependencies.
3. Document setup, deployment, user workflows, privacy boundaries and third-party assets without promising precise offline addresses or automatic iOS album writes.
4. Capture actual desktop/mobile pages and template exports from existing synthetic example scenes. Preserve a reproducible capture script.
5. Run tests/build and production asset checks, inspect staged files, push to the requested repository and read back its commit.

Verification: npm ci, npm test, npm run build, npm run demo; remote main must match local HEAD. iOS system share still requires physical-device verification.
